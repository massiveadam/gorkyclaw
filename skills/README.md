# Skills

Repo-local skills live here so multiple NanoClaw users can share and version-control them.

Each skill should contain:

- `SKILL.md` (required)
- optional helper files under `scripts/`, `assets/`, `references/`

See `skills/templates/basic-skill` for a minimal starting point.
